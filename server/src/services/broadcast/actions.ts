
export async function sendBroadcastMessage(broadcastId, message) {
  // const broadcast = await findBroadcastById(broadcastId);

  // if (!broadcast) {
  //   throw new Error("Broadcast not found");
  // }

  // broadcast.messages.push(message);

  // await broadcast.save();
  return null;
}
